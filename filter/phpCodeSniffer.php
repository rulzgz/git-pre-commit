<?php

	echo 'CodeSniffer cargado'."\n";
	/*
    // PHP CodeSniffer ************************************************************************************
	$cs_output = array();
	$cs_return = 0;
	exec("{$phpCS} " . escapeshellarg($cwd .'\\'. $file), $cs_output, $cs_return);

	// ¿Hay algún error? Lo mostramos y cancelamos el commit
    if ($cs_return != 0)
    {
    	echo "--( CodeSniffer )---------------------------------------------------------------";
    	echo implode("\n", $cs_output)."\n";
    	$exit_status = 1;
    } 
    // ****************************************************************************************************
    */